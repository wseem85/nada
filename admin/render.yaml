services:
  - type: static
    name: your-app-name
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    # This is the key line for SPAs:
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
